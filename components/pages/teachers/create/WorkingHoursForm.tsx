import React from "react";
import WeeklyWorkingHours from "./WeeklyWorkingHours";

export const WorkingHoursForm = ({
  setActiveStep,
}: {
  setActiveStep: React.Dispatch<React.SetStateAction<number>>;
}) => {
  return (
    <div>Working Hours Form</div>
  );
};
